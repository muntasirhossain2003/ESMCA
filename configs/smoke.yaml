# Tiny config purely for smoke-testing the pipeline end to end.
backbone: roberta-base
max_seq_len: 64
lora_rank: 4
lora_alpha: 8
subjects:
  - abstract_algebra
  - anatomy
batch_size: 4
epochs_per_task: 1
lr: 0.0001
n_prototype_samples: 8
ig_steps: 4
router_tau: 0.1
drift_delta: 0.05
attrib_loss_weight: 1.0
consolidation_lambda: 1.0
ewc_lambda: 1000.0
seed: 42
device: cpu
output_dir: outputs/smoke
